package com.miche.krak.kBot.utils

/**
 * Chat
 */
enum class Target {
    USER, GROUP, CHANNEL
}

/**
 * User
 */
enum class Status {
    BANNED, NOT_REGISTERED, USER, POWER_USER, ADMIN, DEV, CREATOR
}